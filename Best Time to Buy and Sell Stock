class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int minFar=INT_MAX;
        int ans=0;
        for(int i=0;i<prices.size();i++){
            minFar=min(minFar,prices[i]);
            ans=max(ans,prices[i]-minFar);
        }
        return ans;
    }
};
